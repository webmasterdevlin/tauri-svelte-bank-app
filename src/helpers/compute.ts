// math helpers

const depositMoney = (a: number, b: number): number => {
  return a + b;
}

const withdrawMoney = (a: number, b: number): number => {
  return a - b;
}


export {
  depositMoney,
  withdrawMoney,
}
